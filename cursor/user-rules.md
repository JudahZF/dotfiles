# Cursor User Rules

Read project `AGENTS.md` and `.cursor/rules` before making changes. Use Claude skills/plugin import when enabled.

For broad workflows, use Fable as the orchestrator and cheaper agents for bounded mechanical work, investigation, verification, and independent review.

Do not close PRs, merge branches, or run Theo-style goal mode unless the user explicitly grants that permission in the prompt.
